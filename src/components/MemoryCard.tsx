import {Pencil,Trash2} from 'lucide-react'; import type {MemoryEntry} from '../types'; export default function MemoryCard({memory,onEdit,onDelete}:{memory:MemoryEntry;onEdit:()=>void;onDelete:()=>void}){return <article className="rounded-3xl border border-border bg-white p-5 shadow-soft"><div className="flex items-start justify-between gap-4"><div><span className="rounded-full bg-rose px-3 py-1 text-xs font-semibold text-ink">{memory.category}</span><h3 className="serif mt-3 text-xl font-semibold">{memory.prompt}</h3></div><div className="flex gap-1"><button aria-label="Edit memory" onClick={onEdit} className="rounded-xl p-2 hover:bg-paper"><Pencil size={16}/></button><button aria-label="Delete memory" onClick={onDelete} className="rounded-xl p-2 text-terracotta hover:bg-rose"><Trash2 size={16}/></button></div></div><p className="prose-readable mt-4 text-[15px] text-ink/90">{memory.response}</p>{(memory.yearOrEra||memory.tags.length>0)&&<div className="mt-4 flex flex-wrap gap-2 text-xs text-muted">{memory.yearOrEra&&<span className="rounded-full bg-paper px-3 py-1">{memory.yearOrEra}</span>}{memory.tags.map(t=><span key={t} className="rounded-full bg-paper px-3 py-1">#{t}</span>)}</div>}</article>}
