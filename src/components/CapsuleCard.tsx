import {Link} from 'react-router-dom'; import {ArrowUpRight,Clock3} from 'lucide-react'; import type {Capsule} from '../types'; export default function CapsuleCard({capsule}:{capsule:Capsule}){const progress=Math.min(100,capsule.entries.length/12*100);return <Link to={`/capsule/${capsule.id}`} className="group block rounded-3xl border border-border bg-white p-6 shadow-soft transition hover:-translate-y-0.5"><div className="mb-5 flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-forest">{capsule.entries.length} memories</p><h3 className="serif mt-2 text-2xl font-semibold">{capsule.title}</h3></div><span className="grid h-10 w-10 place-items-center rounded-2xl bg-paper transition group-hover:bg-sage"><ArrowUpRight size={18}/></span></div><p className="line-clamp-2 min-h-12 text-sm leading-6 text-muted">{capsule.description||`A record of ${capsule.authorName}'s memories and meaning.`}</p><div className="mt-6"><div className="mb-2 flex justify-between text-xs text-muted"><span>Preserving</span><span>{Math.round(progress)}%</span></div><div className="h-2 overflow-hidden rounded-full bg-paper"><div className="h-full rounded-full bg-forest" style={{width:`${progress}%`}}/></div></div><div className="mt-5 flex items-center gap-2 text-xs text-muted"><Clock3 size={14}/>Updated {new Date(capsule.updatedAt).toLocaleDateString()}</div></Link>}
