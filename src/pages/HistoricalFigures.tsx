import {useNavigate} from 'react-router-dom'; import {BookMarked,Landmark,ArrowRight} from 'lucide-react'; import {historicalCapsules} from '../data/historicalFigures'; import {addHistoricalCapsule} from '../lib/storage';
export default function HistoricalFigures(){const navigate=useNavigate();const open=(id:string)=>{const capsule=addHistoricalCapsule(id);if(capsule)navigate(`/capsule/${capsule.id}`)};return <div className="mx-auto max-w-6xl px-4 py-12 sm:px-6"><p className="text-sm font-semibold uppercase tracking-[.18em] text-forest">Historical learning library</p><h1 className="serif mt-3 text-5xl font-semibold">Explore a life through its recorded history.</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-muted">Open a curated starter capsule to explore documented milestones, themes, and context. The chat discusses the saved record—it does not imitate or claim to speak as the historical person.</p><div className="mt-10 grid gap-6 md:grid-cols-2">{historicalCapsules.map((figure,index)=><article key={figure.id} className="overflow-hidden rounded-[2rem] border border-border bg-white shadow-soft"><div className={`p-8 ${index===0?'bg-sage/35':'bg-rose/45'}`}><Landmark className="text-forest"/><p className="mt-7 text-sm font-semibold uppercase tracking-[.15em] text-muted">Educational archive</p><h2 className="serif mt-2 text-4xl font-semibold">{figure.authorName}</h2><p className="mt-3 leading-7 text-muted">{figure.birthYear} · {figure.entries.length} documented starter entries</p></div><div className="p-7"><h3 className="font-semibold">{figure.title}</h3><p className="mt-3 min-h-20 leading-7 text-muted">{figure.description}</p><div className="mt-5 flex flex-wrap gap-2">{figure.entries.flatMap(e=>e.tags).slice(0,4).map(tag=><span key={tag} className="rounded-full bg-paper px-3 py-1 text-xs text-muted">{tag}</span>)}</div><button onClick={()=>open(figure.id)} className="mt-7 rounded-xl bg-ink px-5 py-3 text-sm font-semibold text-white">Open learning capsule <ArrowRight className="ml-1 inline" size={16}/></button></div></article>)}</div><p className="mt-8 flex gap-2 rounded-2xl border border-border bg-paper p-4 text-sm leading-6 text-muted"><BookMarked className="mt-1 shrink-0 text-forest" size={18}/>These capsules are concise educational starters based on widely documented public information. Add sources and entries before using them for research or making factual claims.</p></div>}
