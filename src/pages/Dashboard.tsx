import {Link,useSearchParams} from 'react-router-dom'; import {Plus,Search,Sparkles} from 'lucide-react'; import {useMemo,useState} from 'react'; import CapsuleCard from '../components/CapsuleCard'; import {useCapsules} from '../hooks/useCapsules'; export default function Dashboard(){const {capsules}=useCapsules(); const [q,setQ]=useState(''); const [sp]=useSearchParams(); const list=useMemo(()=>capsules.filter(c=>`${c.title} ${c.authorName} ${c.createdFor}`.toLowerCase().includes(q.toLowerCase())),[capsules,q]);return <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="text-sm font-semibold text-forest">Welcome back</p><h1 className="serif mt-2 text-4xl font-semibold sm:text-5xl">Your time capsules</h1><p className="mt-3 max-w-2xl text-muted">A quiet place for stories that deserve to outlast a moment.</p></div><Link to="/create" className="rounded-2xl bg-ink px-5 py-3 text-sm font-semibold text-white hover:bg-forest"><Plus className="mr-2 inline" size={17}/>Create New Capsule</Link></div><div className="mt-8 flex flex-col gap-3 rounded-3xl border border-border bg-white p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3.5 text-muted" size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search capsules, people, or research context" className="w-full rounded-2xl bg-paper py-3 pl-10 pr-4 outline-none"/></div><div className="flex items-center gap-2 rounded-2xl bg-sage/20 px-4 py-3 text-sm text-forest"><Sparkles size={16}/>Local-first preservation</div></div>{sp.get('sample')&&<div className="mt-6 rounded-3xl border border-sage bg-sage/15 p-5 text-sm">The sample capsule is ready to explore. Everything you create here remains in this browser.</div>}<section className="mt-10"><div className="mb-5 flex items-center justify-between"><h2 className="serif text-2xl font-semibold">Continue preserving</h2><span className="text-sm text-muted">{list.length} capsule{list.length!==1?'s':''}</span></div>{list.length?<div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{list.map(c=><CapsuleCard key={c.id} capsule={c}/>)}</div>:<div className="rounded-3xl border border-dashed border-border bg-white p-12 text-center"><h3 className="serif text-2xl font-semibold">Nothing matches yet</h3><p className="mx-auto mt-2 max-w-md text-muted">Try another search, or start a new capsule for someone whose story matters.</p></div>}</section></div>}
